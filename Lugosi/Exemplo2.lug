main{
    var bool verdadeiro;
    var int count;
    var float max;

    verdadeiro := true;
    count := 0;
    max := 20;

    if ((verdadeiro == true)) {
        count := (count + 1);
    };

    do {
        count := (count + 1);
        print(count);
    } while ((count < max));
}